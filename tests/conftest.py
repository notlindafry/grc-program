"""Shared pytest configuration.

The GRC-ecosystem test suites (``test_graph``, ``test_graph_engine``) build their
own minimal graphs and fixtures inline, so no shared builders live here after the
legacy v1 retirement (SPEC v2.2 §C4).
"""
